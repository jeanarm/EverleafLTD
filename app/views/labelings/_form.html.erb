<%= form_with(model: labeling, local: true) do |form| %>
  <% if labeling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(labeling.errors.count, "error") %> prohibited this labeling from being saved:</h2>

      <ul>
      <% labeling.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :label_id %>
    <%= form.text_field :label_id %>
  </div>

  <div class="field">
    <%= form.label :task_id %>
    <%= form.text_field :task_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
